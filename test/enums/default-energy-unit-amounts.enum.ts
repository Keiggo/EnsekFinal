export enum DefaultEnergyUnitAmounts {
    Electric = 5000,
    Gas = 5000,
    Nuclear = 0,
    Oil = 5000

    //since the reset doesn't work, there's no way for me to know what the default amounts actually are
}